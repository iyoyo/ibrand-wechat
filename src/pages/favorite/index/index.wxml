<view id="favorite-index">
    <checkbox-group bindchange="select">
    <view class="num-box">
        <view class="goods">您收藏了 <text class="num">{{total}}</text> 个商品</view>
        <view class="operation" bindtap="edit">{{edit ? '完成' : '编辑'}}</view>
    </view>
    <view class="goods-list">
            <view wx:for="{{dataList}}" wx:for-index="idx">
                <view class="goods-item mx-1px-bottom" wx:if="{{!item.remove}}" wx:for="{{dataList[idx]}}">
                    <view class="img-box">
                        <checkbox  checked="{{item.isCheck}}" data-ischecked="{{item.isCheck}}" data-findex="{{idx}}" data-index="{{index}}" data-value="{{item.favoriteable_id}}" value="{{item.favoriteable_id}}" />
                        <image src="{{item.favoriteable.img}}" />
                    </view>
                    <view class="text">
                        <view class="names">{{item.favoriteable.name}}</view>
                        <view class="money">¥{{item.favoriteable.sell_price}}</view>
                    </view>
                </view>
            </view>
    </view>
    </checkbox-group>
    <view class="cancel mx-1px-top">
        <view class="checkbox">
            <label>
                <checkbox bindtap="click" checked="{{allCheck}}" /> 全选
            </label>
        </view>
        <view class="cancel-button" bindtap="cancel">
            取消收藏
        </view>

    </view>
</view>